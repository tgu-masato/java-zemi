version: '3'

services:
  app:
    build:
      context: ./containers/app
    working_dir: /java_app_sample
    volumes:
      - ./:/java_app_sample
      - ./build/libs/:/usr/local/tomcat/webapps/
    ports:
      - "8080:8080"
